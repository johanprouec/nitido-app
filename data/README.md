# Data

Dataset del reto:

```text
candidatos_nitido.csv
```

Este archivo debe contener 5000 candidatos, las variables `x1` a `x18` y la etiqueta binaria `avanza`.

Estado actual:

- `candidatos_nitido.csv` cargado y verificado: 5000 filas, 19 columnas.
